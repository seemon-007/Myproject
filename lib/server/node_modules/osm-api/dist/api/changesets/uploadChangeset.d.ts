import type { OsmChange } from "../../types";
/**
 * uploads a changeset to the OSM API.
 * @returns the changeset number
 */
export declare function uploadChangeset(tags: {
    [key: string]: string;
}, diff: OsmChange): Promise<number>;
